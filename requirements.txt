flask>=3.0
# Servidor WSGI de producao. O app.run() do Flask e servidor de desenvolvimento
# -- a propria documentacao diz para nao expor. waitress e Python puro, roda no
# Windows e empacota no PyInstaller sem depender de compilador.
waitress>=3.0
moviepy>=2.1
requests>=2.32
python-dotenv>=1.0
Pillow>=11.2,<12
numpy>=1.25
imageio-ffmpeg>=0.4.9

# Empacotamento do backend como executavel sidecar (Electron)
pyinstaller>=6.6
